#def Handler(res):
    #if(res['error']['message'])



'''

    #if rate limit is triggered, back-off retry
    #if(req.status_code == 429):
    #   print('Rate limit triggered, pausing for ' + str(res['Retry-After']) + 'seconds')
    #  time.sleep(res['Retry-After'])
    
    if(req.status_code != 200):
        return(res['error']['message'])


    print ('Status:' + str(req.status_code))
'''
    